// copypasted from https://developer.android.com/studio/publish/app-signing.html for signing
def keystorePropertiesFile = rootProject.file("keystore.properties")

ext {
    keystore_properties = new Properties()
    keystore_properties.load(new FileInputStream(keystorePropertiesFile))
}